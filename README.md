# react-setup
It contains basic setup for react to start with an App

## steps
1. mkdir react-setup
2. git init // To intialise git repo
3. git pull url
4. pipenv shell // Start vitual env
5. npm install // Install npm dependency in virtual env
6. npm start // To build and start your application
